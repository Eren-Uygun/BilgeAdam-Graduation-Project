@model PharmaceuticalWarehouseManagementSystem.ENTITY.Entity.OrderDetail

@{
    ViewData["Title"] = "Add";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}



<div class="card-header">
   
    <h4>OrderDetail Add</h4>
</div>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Add">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OrderID" class="control-label"></label>
                <select asp-for="OrderID" class="form-control" asp-items="@(new SelectList(ViewBag.OrderID,"ID","ID".ToString()))"></select>
            </div>
            <div class="form-group">
                <label asp-for="ProductID" class="control-label"></label>
                <select asp-for="ProductID" class="form-control" asp-items="@(new SelectList(ViewBag.ProductID,"ProductName","ProductName"))"></select>
            </div>
            <div class="form-group">
                <label asp-for="ShipperID" class="control-label"></label>
                <select asp-for="ShipperID" class="form-control" asp-items="@(new SelectList(ViewBag.ShipperID,"CompanyName","CompanyName"))"></select>
            </div>
            <div class="form-group">
                <label asp-for="UnitPrice" class="control-label"></label>
                <input asp-for="UnitPrice" class="form-control" />
                <span asp-validation-for="UnitPrice" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Quantity" class="control-label"></label>
                <input asp-for="Quantity" class="form-control" />
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Discount" class="control-label"></label>
                <input asp-for="Discount" class="form-control" />
                <span asp-validation-for="Discount" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" id="Add" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="List">Back to List</a>
</div>

<script>
    document
        .getElementById("Add")
        .addEventListener("click", function (e) { //e => event
            if (!confirm("Do you want to Add?")) {
                e.preventDefault(); // ! => don't want to do this
            } else {
                //want to do this! => maybe do something about it?

            }
        });

</script>
